# Generated / derived — regenerate from your own Deadlock install
extracted/

# Python
__pycache__/
*.pyc
*.pyo

# Third-party binaries — fetch from upstream
# (Source2Viewer CLI from github.com/ValveResourceFormat/ValveResourceFormat/releases)
tools/vrf/

# Local tooling config
.claude/
Knowledge/

# OS
.DS_Store
Thumbs.db
desktop.ini
upload.out

# Intermediate pipeline outputs — regenerable from src/ + a Deadlock install.
# Per-hero TTLs and the dev viewer assets are not tracked; only the final
# self-contained bundle (outputs/nydus.html) is committed for GitHub Pages.
outputs/heroes/
outputs/viewer/

# Dev / pipeline scratch outputs (regenerable, not consumer-facing)
outputs/roster.jsonl
outputs/roster_intelligence.md
outputs/frequency_report.md
outputs/rubric.md
outputs/stat_bridge.jsonl
outputs/stat_bridge_summary.md
outputs/unknowns.jsonl
outputs/wander_questions.md
outputs/proof.html
outputs/abrams.ttl
outputs/infernus.ttl
