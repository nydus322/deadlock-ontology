// Tests are organised into sub-directories:
//   test/unit/   — pure-Dart unit tests (PropFormatter, HierarchicalLayout, TopologyExtractor)
//   test/widget/ — Flutter widget tests (TagChip, StatRow, SectionHeader, TierStamp)
//
// Run all tests:
//   flutter test
//
// Run a specific suite:
//   flutter test test/unit/prop_formatter_test.dart

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test entry point — run flutter test to execute all suites', () {
    expect(true, isTrue);
  });
}
